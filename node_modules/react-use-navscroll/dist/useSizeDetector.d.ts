import { useNavScrollArgs } from './types';
export declare type useSizeDetectorArgs = Omit<useNavScrollArgs, 'offset'> & {
    activeId: string | null;
    hasWindow: boolean;
    setForceRecompute: (force: boolean) => void;
    updateActiveId: (id: string | null) => void;
};
export declare const useSizeDetector: ({ root, isHorizontal, activeId, onChange, setForceRecompute, updateActiveId, hasWindow }: useSizeDetectorArgs) => {
    targetSize: number;
    useViewport: boolean;
};
