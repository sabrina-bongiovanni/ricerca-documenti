import { ReactChild } from 'react';
import type { NotificationOptions } from './NotificationContent';
import { NotificationId } from './types';
export declare function notify(title: string, options?: NotificationOptions): NotificationId;
export declare namespace notify {
    var dismiss: (id?: string | undefined) => void;
}
export declare function notify(title: string, body: ReactChild, options?: NotificationOptions): NotificationId;
export declare namespace notify {
    var dismiss: (id?: string | undefined) => void;
}
