import { Component, HTMLAttributes } from 'react';
export interface PasswordInputProps extends HTMLAttributes<HTMLInputElement> {
    type: 'password' | HTMLInputElement['type'];
}
interface PasswordInputState {
    showPassword: boolean;
}
export declare class PasswordInput extends Component<PasswordInputProps, PasswordInputState> {
    state: {
        showPassword: boolean;
    };
    onPasswordToggle: () => void;
    render(): JSX.Element;
}
export {};
