import * as React from 'react';
import { IScroll, IDimensions } from './index';
import { PriorityType } from './types';
interface InjectedProps {
    scroll?: IScroll | null;
    dimensions?: IDimensions | null;
}
declare type OmitValues = 'scroll' | 'dimensions';
interface IOptions {
    omit?: OmitValues[];
    deferUpdateUntilIdle?: boolean;
    priority?: PriorityType;
}
export default function connect(options?: IOptions): <P extends object>(WrappedComponent: React.ComponentType<P & InjectedProps>) => React.ComponentClass<P, any>;
export {};
