import * as React from 'react';
import { TRenderChildren, IPositionStyles } from './types';
interface IProps<R> {
    disabled: boolean;
    renderArgs?: R;
    children: TRenderChildren<R>;
    forwardRef?: React.RefObject<any>;
    style?: React.CSSProperties;
    positionStyle?: IPositionStyles;
}
declare const StickyElement: <R extends {}>({ children, forwardRef, style: overrideStyles, positionStyle, disabled, renderArgs, ...props }: IProps<R>) => JSX.Element;
export default StickyElement;
