import * as React from 'react';
import { IStickyInjectedProps } from './types';
export declare const connect: () => <P extends object>(WrappedComponent: React.ComponentType<P & IStickyInjectedProps>) => React.SFC<P>;
export default class StickyScrollUpProvider extends React.PureComponent {
    stickyOffset: {
        top: number;
        height: number;
    };
    updateStickyOffset: (stickyOffset: number, height: number) => void;
    render(): JSX.Element;
}
